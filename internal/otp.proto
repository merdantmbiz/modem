syntax = "proto3";

package auth;

option go_package = "./gen;auth";

service OTPService {
  rpc PassOTP (OTPRequest) returns (OTPResponse);
}

message OTPRequest {
  string client_id = 1;
}

message OTPResponse {
  string client_id = 1;
}
